<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!-- Without this, Hardened Runtime blocks mic capture outright before
	     macOS ever gets to a TCC prompt — the app never even appears under
	     System Settings > Privacy & Security > Microphone, regardless of
	     NSMicrophoneUsageDescription or askForMediaAccess. See
	     electron/main.js's askForMediaAccess call, which depends on this. -->
	<key>com.apple.security.device.audio-input</key>
	<true/>
</dict>
</plist>
