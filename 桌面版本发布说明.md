# 桌面版本打包与更新

本项目产出两个安装文件：Windows Intel/AMD 电脑使用 `JobCompass-版本-win-x64.exe`；Apple Silicon Mac 使用 `JobCompass-版本-mac-arm64.dmg`。

## 用户如何更新

首次使用支持更新的新版本时，Windows 需要安装一次新版 EXE。以后在软件里检查更新、下载，再重启安装。请保留 GitHub Release 中的 `latest.yml` 和 `.exe.blockmap`；它们用于版本检查、校验和差分下载。差分下载受网络和服务端文件可用性影响，失败时更新器可能改为完整下载。

Mac 当前点击软件提供的更新入口，下载 ARM64 DMG，将应用替换到应用程序目录。应用做了 ad-hoc 签名（`electron-builder.cjs` 里 `mac.identity: "-"`）——这不需要 Apple 开发者账号，只是满足 Apple Silicon 强制要求的签名，让应用能启动；**不要把它改回 `identity: null`**，那样打出来的包在 arm64 Mac 上会直接提示"已损坏，无法打开"，完全打不开，而不只是没有自动更新（v0.8.0/v0.8.1 就是这样发出去的，已在 v0.8.2 修复）。ad-hoc 签名不等于正式 Developer ID 签名，首次打开仍会提示"无法验证开发者"，右键选"打开"一次即可；也因为不是正式签名+公证，macOS 自动替换更新做不到，因此目前不会承诺 Mac 一键自动安装。后续配置正式签名、公证并添加 ZIP 更新产物后，可继续接入自动更新。

数据与程序分开保存。内部应用名 `careerplatform`、应用 ID `com.careerplatform.local` 不得随版本改变；Windows NSIS 设置明确保留卸载时的应用数据。正常覆盖更新不需要重新填写个人资料或 API Key，也不需要重新导入简历。升级前仍应保留应用生成的数据备份。

## 本机打包

需要 Node.js 22、npm、Git 以及项目依赖。Windows 安装包在 Windows x64 上构建；Mac DMG 在 Apple Silicon Mac 上构建，保证 Prisma 的本机二进制匹配。

```powershell
npm ci
node scripts/packaging-build.cjs
node scripts/packaging-prepare.cjs
npx --no-install electron-builder --config electron-builder.cjs --win --x64 --publish never
```

在 Apple Silicon Mac 上，最后一步换成：

```bash
npx --no-install electron-builder --config electron-builder.cjs --mac --arm64 --publish never
```

产物位于 `dist-electron/`。`--publish never` 保证本机命令只生成文件；它不会提交源码、上传安装包或发布 GitHub Release。

打包脚本先使用临时空数据库、空附件目录和随机临时密钥构建。它不会更改本机 `.env` 或已安装软件的数据。随后只复制 Next standalone 运行入口、生产构建、Git 跟踪的 public 资源、数据库结构与迁移脚本，并补齐 Prisma 命令行运行依赖；Electron 主进程单独放入 ASAR，只携带更新器依赖，避免把完整 node_modules 重复打包。

新 public 图标或其他资源需要先纳入 Git 跟踪，才会进入安装包。开发缓存、测试依赖、SQLite、`.env*`、`.secret`、uploads、backups、`.local-run`、`.local-data` 以及桌面本机配置不进入包。准备与打包完成后都会再次检查私有文件，并检查已知环境密钥是否被编译进文本或 ASAR；错误信息只显示文件路径。

普通 `npm run build` 不产生打包许可标记。修改代码后必须重新执行 `packaging-build.cjs`，不能直接使用可能连接过真实数据的普通构建。

## GitHub 自动生成安装文件

工作流位于 `.github/workflows/desktop-release.yml`。

1. 提交代码前，确保个人数据、备份、环境文件没有纳入 Git；只允许 `.env.example`。原仓库曾跟踪 `.env.vercel-pulled.bak`，需要从源码索引移除。工作流会检查文件路径并拒绝包含这些文件的版本。
2. 在 GitHub Actions 中手动运行 **Desktop installers**，会在 Windows 和 Apple Silicon Mac 机器各构建一次，安装包可从运行结果的 Artifacts 下载；手动运行不会发布版本。
3. 发布正式版本时，先递增 `package.json` 与锁文件的版本并提交，再推送与版本完全一致的标签，例如包版本 `0.8.0` 使用标签 `v0.8.0`。
4. 两个平台都成功后，工作流创建一个包含安装文件和 Windows 更新元数据的 GitHub Release 草稿。检查版本说明与安装效果后，在 GitHub 点击发布；已发布的版本不会被工作流覆盖。
5. Windows 更新检查会发现已发布的新版本。GitHub 草稿不对普通用户显示，不能用于实际更新验证。

## 尚需实际发布后验证

本地静态验证与构建不能代替端到端更新验收。正式使用前，需要用旧安装版升级到新版本，确认版本改变、数据与密钥可继续读取；Mac 还需要在 Apple Silicon 真机打开 DMG 并替换应用。没有签名的安装包也可能触发操作系统提示。

打包依照 [Next standalone 本机部署说明](https://nextjs.org/docs/app/api-reference/config/next-config-js/output)、[electron-builder 文件规则](https://www.electron.build/v26/docs/contents/)、[NSIS 数据保留与安装规则](https://www.electron.build/v26/docs/nsis/) 和 [自动更新要求](https://www.electron.build/docs/features/auto-update/)。构建机架构依据 [GitHub 官方 runner 列表](https://github.com/actions/runner-images/blob/main/README.md)。
