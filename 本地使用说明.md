# 求职罗盘本地版

双击根目录的 **启动求职罗盘.cmd**，或打开 <http://127.0.0.1:3000>。

首次运行已安装项目依赖、完成数据库迁移、预置 19 家企业，并构建生产版本。启动脚本只监听本机地址，运行日志在 `.local-run/`。

## 配置 AI

在「账号设置 → AI 设置」添加自己的服务商 API Key，并设为默认。不要把 Key 粘贴到聊天中。联网研究还需要配置项目支持的搜索服务商 Key（Qwen / Gemini / Claude / OpenAI），普通文本模型可继续用于其他任务。实际可用模型和搜索能力以服务商接口为准。

## 新增 Agent 能力

- 求职管家：查询完整数据库中的候选岗位、招聘线索、投递和待办，整理计划，提出保存操作。
- 岗位研究：按需联网研究公司和岗位，结合已保存的简历体检摘要分析匹配；简历需先完成 AI 体检。
- 投递助手：检查目标岗位所需资料，准备材料草稿并链接网申入口。桌面版现有网申浏览器支持辅助填写；提交仍由用户核对后操作。
- 每轮最多调用 4 个只读工具，显示执行结果和失败原因。所有数据库修改继续通过操作按钮确认。
- 最近 60 条对话文字保存在当前浏览器，刷新后保留。旧操作卡片不恢复，避免重复执行；「新对话」清空这些记录。它不是跨设备同步或永久知识库。

示例：

1. 「查询腾讯的候选岗位，结合我的简历分析匹配，再研究一下公开校招要求。」
2. 「为候选池里的某个岗位准备投递材料，检查缺少什么，并给我网申入口。」
3. 「根据这周的截止日期安排任务，生成我可以保存的待办。」

## 开发与数据

- 开发：`npm run local:dev`（先关闭使用 3000 端口的生产服务）。
- 修改代码后：`npm run desktop:prepare`，然后重新启动服务；已有构建不会自动更新。
- 检查 Agent：`npm run test:agent`。
- 桌面开发：`npm run electron:dev`，默认端口 3210。
- 这台电脑的网页部署已经接入桌面数据目录 `%APPDATA%/careerplatform/`，数据库、附件和 AI Key 共用一份；不要同时在两个窗口修改同一条记录。
- 项目内的 `.env` 和 `.local-desktop.json` 只保存本机连接信息，均被 Git 忽略。升级备份在 `.local-data/backups/`，也不会上传到 GitHub。

已通过生产构建、独立运行时、数据升级和离线工具测试。没有主动调用你已配置的真实 AI Key，因此尚未验证实际服务商响应、联网检索质量及真实招聘网站的填写效果。

原项目构建会提示动态文件路径追踪警告；安装时 npm 报告 10 项依赖漏洞（3 中危、7 高危），本次未进行可能破坏兼容性的跨版本强制升级。
